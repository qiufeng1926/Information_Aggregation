# 开发服务器端口（默认 5173）
# VITE_DEV_PORT=5173

# Vite 代理目标（同机开发保持默认即可）
VITE_API_TARGET=http://127.0.0.1:8000

# 内网穿透（cpolar/ngrok）仅暴露前端时，保持默认即可（/api 由 Vite 代理到本机后端）
# 若单独穿透后端，需在后端 CORS_ORIGINS 加入穿透域名
# 设置后 axios 将直接请求该地址，需后端 CORS 已放行
# VITE_API_BASE_URL=http://192.168.1.100:8000/api/v1
